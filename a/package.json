{
  "name": "a",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && vite gh-pages"
  },
  "devDependencies": {
    "vite": "^6.3.1"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
